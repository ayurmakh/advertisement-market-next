services:
  db:
    image: postgres:16
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - advertisement_market_next_db_data:/var/lib/postgresql/data

volumes:
  advertisement_market_next_db_data:
